#NU MERGE!!!

prop = input ("Propozitia: ")
s = input ("Cuvantul vechi: ")
t = input ("Cuvantul nou: ")









'''
poz = prop.find (s)
k = len (s)
while poz < len (prop):
    if (poz == 0 or prop[poz - 1].isalpha() == False) or (poz + k == len (prop) or prop[poz + k].isapla() == False):
        prop = prop[:poz] + prop[poz:].replace (s, t, 1)
    poz = poz + k + prop[poz + k:].find (s)
print (prop)
'''