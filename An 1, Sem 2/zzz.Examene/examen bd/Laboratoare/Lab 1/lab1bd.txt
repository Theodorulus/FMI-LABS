SELECT * FROM employees;

SELECT * FROM DEPARTMENTS;

SELECT * from job_history;

cheia primara -> este o coloana sau o combinatie de coloane
              -> este un identificator unic(ESTE UNICA)
              -> nu poate fi null
            
cheia externa -> este o coloana
              -> se poate repeta
              ->poate fi null
        
select job_id from employees; --107 joburi deoarece job_id este cheia externa deci se poate repeta
                              -107 joburi deoarece avem 107 angajati
                            
select distinct job_id from employees;

select unique job_id from employees;

desc employees;

ex.8, pag. 2:
/*SELECT employee_id, last_name salary * 12 ANNUAL SALARY
FROM exmployees;*/
SELECT employee_id ID, last_name as Nume, salary * 12 ANNUAL_SALARY
FROM employees;

ex.6:
select last_name || ', ' || job_id -- || inseamna concatenare
from employees;

--tema ex bolduit

ex.7:
select last_name, salary
from employees
where salary>2850;

ex.9:
select last_name, salary
from employees
where salary between 1500 and 2850;
--sau
select last_name, salary
from employees
where salary>1500 and salary<2850;

TEMA:
ex. bolduit.
select employee_id || ', ' || first_name || ', ' || last_name || ', ' || email || ', ' || phone_number || ', ' || hire_date || ', ' || job_id || ', ' || salary || ', ' || commission_pct || ', ' || manager_id || ', ' || department_id "Informatii complete"
from employees;

8.
select first_name, last_name, department_id
from employees
where employee_id = 104;
10.
select first_name, last_name, job_id
from employees
where hire_date between '20-feb-1987' and '1-may-1989'
order by hire_date;